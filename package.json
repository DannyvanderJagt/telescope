{
  "name": "suite",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && babel src --out-dir dist -w",
    "start": "node ."
  },
  "author": "The Suitcase",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0"
  },
  "dependencies": {
    "http": "0.0.0",
    "rethinkdb-websocket-server": "^0.6.0"
  }
}
